# 前端页面打不开 - 问题排查指南

## 🔍 可能的原因

### 1. 前端服务未启动
**症状**: 访问 http://localhost:5173 显示"无法访问此网站"

**解决方案**:
```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历/frontend
npm run dev
```

### 2. 依赖未安装
**症状**: 运行 `npm run dev` 报错

**解决方案**:
```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历/frontend
npm install
npm run dev
```

### 3. 端口被占用
**症状**: 提示端口 5173 已被占用

**解决方案**:
```bash
# 查找占用端口的进程
lsof -i :5173

# 杀死进程（替换 PID 为实际进程号）
kill -9 PID

# 或者修改端口
npm run dev -- --port 5174
```

### 4. React Router 版本问题
**症状**: 页面空白或路由不工作

**解决方案**: 检查 React Router 版本兼容性

---

## ✅ 快速诊断步骤

### 步骤 1: 检查文件是否存在
```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历/frontend

# 检查关键文件
ls -la src/App.tsx
ls -la src/pages/auth/Login.tsx
ls -la src/pages/auth/Register.tsx
ls -la src/pages/Dashboard.tsx
```

### 步骤 2: 检查依赖
```bash
ls -la node_modules
```

如果 node_modules 不存在，运行：
```bash
npm install
```

### 步骤 3: 启动开发服务器
```bash
npm run dev
```

### 步骤 4: 查看控制台错误
打开浏览器开发者工具（F12），查看 Console 标签页的错误信息。

---

## 🐛 常见错误及解决方案

### 错误 1: "Cannot find module"
```
Error: Cannot find module './pages/auth/Login'
```

**原因**: 文件路径错误或文件不存在

**解决**: 检查文件是否存在，路径是否正确

### 错误 2: "Unexpected token"
```
SyntaxError: Unexpected token '<'
```

**原因**: JSX 语法错误或 TypeScript 配置问题

**解决**: 检查 tsconfig.json 和 vite.config.ts

### 错误 3: 页面空白
**原因**: 
- React Router 配置错误
- 组件渲染错误
- CSS 加载失败

**解决**: 
1. 打开浏览器控制台查看错误
2. 检查 App.tsx 中的路由配置
3. 检查组件是否正确导入

---

## 🔧 完整重置步骤

如果以上方法都不行，尝试完全重置：

```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历/frontend

# 1. 删除 node_modules 和 lock 文件
rm -rf node_modules package-lock.json

# 2. 清除 npm 缓存
npm cache clean --force

# 3. 重新安装依赖
npm install

# 4. 启动开发服务器
npm run dev
```

---

## 📋 检查清单

- [ ] 前端目录存在: `/Users/robinxie/01-开发项目/AI 智能简历/frontend`
- [ ] package.json 存在
- [ ] node_modules 已安装
- [ ] src/App.tsx 存在
- [ ] src/pages/auth/Login.tsx 存在
- [ ] src/pages/auth/Register.tsx 存在
- [ ] src/pages/Dashboard.tsx 存在
- [ ] 开发服务器已启动
- [ ] 浏览器可以访问 http://localhost:5173
- [ ] 浏览器控制台无错误

---

## 🆘 如果还是不行

### 方法 1: 使用 Docker
```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历
docker-compose up frontend
```

### 方法 2: 查看详细日志
```bash
cd frontend
npm run dev 2>&1 | tee frontend-error.log
```

然后查看 `frontend-error.log` 文件中的错误信息。

---

## 💡 当前状态检查命令

运行以下命令检查当前状态：

```bash
cd /Users/robinxie/01-开发项目/AI\ 智能简历/frontend

echo "=== 检查 package.json ==="
cat package.json | grep -A 5 '"scripts"'

echo -e "\n=== 检查文件结构 ==="
tree -L 3 src/ 2>/dev/null || find src -type f

echo -e "\n=== 检查进程 ==="
lsof -i :5173

echo -e "\n=== 尝试启动 ==="
npm run dev
```

---

**最后更新**: 2026-02-05 19:52
